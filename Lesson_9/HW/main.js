let two_digit_number = prompt("Type a two-digit number: ");

let first_num = two_digit_number.charAt(0);

let second_num = two_digit_number.charAt(1);

let answer = (+first_num) + (+second_num);

alert(answer);
